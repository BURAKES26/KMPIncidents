package com.example.kmpincidents.di

import org.koin.dsl.module

// Platform-specific Android bindings can be added here.
// Shared expect classes (TokenPreferences, IncidentDataStore, PhotoFileResolver)
// are registered in common dataModule.
val androidDataModule = module { }
